services:
  app:
    volumes:
      # Mount .bare at the same host absolute path so git worktree refs work
      - /Users/blasin/Works/BSNexus/.bare:/Users/blasin/Works/BSNexus/.bare:cached
  postgres:
    ports:
      - "${EXTERNAL_PG_PORT:-5432}:5432"
  redis:
    ports:
      - "${EXTERNAL_REDIS_PORT:-6379}:6379"
