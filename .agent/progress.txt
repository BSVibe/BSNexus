# Progress Log
Started: 2026-03-28T09:00:23Z

## TASK-001: Define ExecutorProtocol with typing.Protocol
- Replaced `BaseExecutor(ABC)` with `ExecutorProtocol(Protocol)` using `@runtime_checkable`
- Added `supported_task_types() -> list[str]` method to the protocol
- Kept `BaseExecutor` as backward-compatible alias (`BaseExecutor = ExecutorProtocol`)
- Removed ABC inheritance from `ClaudeCodeExecutor`, added `supported_task_types()` returning `["coding", "refactor", "bugfix", "test"]`
- Updated `__init__.py` to export `ExecutorProtocol`
- All 879 existing tests still pass, coverage 93.57%
- 8 new protocol tests in `backend/tests/core/executor/test_protocol.py`
- Files changed: `base.py`, `claude_code.py`, `__init__.py`
- Key decision: `BaseExecutor = ExecutorProtocol` alias preserves all existing imports in task_runner.py and tests without changes

## TASK-002: Create ExecutorRegistry
- Created `backend/src/core/executor/registry.py` with thread-safe singleton `ExecutorRegistry`
- Methods: `register(name, factory)`, `get(name)` (creates new instance each call), `list_available()`
- Raises `KeyError` for duplicate registration and unknown executor lookup
- TDD: 7 tests written first in `backend/tests/core/executor/test_registry.py`
- Exported `ExecutorRegistry` from `__init__.py`
- All 886 tests pass, coverage 93.61%, ruff clean
- Files changed: `registry.py` (new), `__init__.py` (updated), `test_registry.py` (new)

## TASK-003: Refactor ClaudeCodeExecutor to implement ExecutorProtocol
- ClaudeCodeExecutor already satisfies ExecutorProtocol structurally (execute, review, supported_task_types methods added in TASK-001)
- Registered ClaudeCodeExecutor in ExecutorRegistry at module init (`__init__.py`)
- Rewrote `create_executor()` to delegate to `ExecutorRegistry.get()` instead of hardcoded if/else
- Changed canonical executor name from `"claude-code"` to `"claude_code"` (underscores, matching Python conventions)
- Updated `config.py` default `executor_type` from `"claude-code"` to `"claude_code"`
- Fixed singleton test isolation bug: `ExecutorRegistry.__new__()` returned the singleton, so test fixtures clearing `_executors` corrupted global state. Fixed by using `object.__new__()` in test fixtures.
- TDD: 11 new tests in `backend/tests/core/executor/test_claude_code_protocol.py` (protocol compliance, registration, create_executor integration)
- Updated existing tests in `test_executor_claude_code.py` to use new executor name and KeyError
- All 897 tests pass, coverage 93.64%, ruff clean
- Files changed: `__init__.py` (updated), `config.py` (updated), `test_claude_code_protocol.py` (new), `test_registry.py` (fixture fix), `test_executor_claude_code.py` (updated)

## TASK-004: Add executor_type and executor_metadata fields to Task model
- Added `executor_type: str` (default='coding', server_default='coding') to Task model for executor routing
- Added `executor_metadata: dict` (default={}, server_default='{}') to Task model for executor-specific configuration
- Named `executor_metadata` instead of `metadata` because SQLAlchemy reserves `metadata` attribute name on declarative models
- Created Alembic migration `l4g5h6i7j8k9_add_executor_type_and_metadata_to_tasks.py`
- Updated schemas: `TaskCreate`, `TaskUpdate`, `TaskResponse` all include both new fields with defaults
- TDD: 15 tests in `backend/tests/test_task_executor_fields.py` (5 model, 10 schema)
- All 923 tests pass, coverage 93.65%, ruff clean
- Files changed: `models.py`, `schemas.py`, migration file (new), `test_task_executor_fields.py` (new)

## TASK-005: Update TaskRunner to use ExecutorRegistry
- Changed `LocalTaskRunner.__init__` from `executor: BaseExecutor` to `executor_name: str = "claude_code"` with optional `executor:` kwarg for backward compat
- Added `_resolve_executor(executor_type)` method: tries task's executor_type from registry, falls back to default executor_name
- `execute_task` and `review_task` now read `task.executor_type` and resolve executor per-task via registry
- Updated callers: `pm.py` and `cli/_helpers.py` now pass `executor_name=settings.executor_type` instead of `create_executor()`
- Backward compatible: existing tests pass unchanged using `executor=` kwarg
- TDD: 10 new tests in `backend/tests/test_task_runner_registry.py` (init, resolve, execute, review, backward compat)
- All 922 tests pass, coverage 93.65%, ruff clean
- Files changed: `task_runner.py` (updated), `pm.py` (updated), `cli/_helpers.py` (updated), `test_task_runner.py` (updated imports), `test_task_runner_registry.py` (new)
