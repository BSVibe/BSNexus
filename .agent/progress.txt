# Progress Log
Started: 2026-03-28T09:00:23Z

## TASK-001: Define ExecutorProtocol with typing.Protocol
- Replaced `BaseExecutor(ABC)` with `ExecutorProtocol(Protocol)` using `@runtime_checkable`
- Added `supported_task_types() -> list[str]` method to the protocol
- Kept `BaseExecutor` as backward-compatible alias (`BaseExecutor = ExecutorProtocol`)
- Removed ABC inheritance from `ClaudeCodeExecutor`, added `supported_task_types()` returning `["coding", "refactor", "bugfix", "test"]`
- Updated `__init__.py` to export `ExecutorProtocol`
- All 879 existing tests still pass, coverage 93.57%
- 8 new protocol tests in `backend/tests/core/executor/test_protocol.py`
- Files changed: `base.py`, `claude_code.py`, `__init__.py`
- Key decision: `BaseExecutor = ExecutorProtocol` alias preserves all existing imports in task_runner.py and tests without changes

## TASK-002: Create ExecutorRegistry
- Created `backend/src/core/executor/registry.py` with thread-safe singleton `ExecutorRegistry`
- Methods: `register(name, factory)`, `get(name)` (creates new instance each call), `list_available()`
- Raises `KeyError` for duplicate registration and unknown executor lookup
- TDD: 7 tests written first in `backend/tests/core/executor/test_registry.py`
- Exported `ExecutorRegistry` from `__init__.py`
- All 886 tests pass, coverage 93.61%, ruff clean
- Files changed: `registry.py` (new), `__init__.py` (updated), `test_registry.py` (new)

## TASK-003: Refactor ClaudeCodeExecutor to implement ExecutorProtocol
- ClaudeCodeExecutor already satisfies ExecutorProtocol structurally (execute, review, supported_task_types methods added in TASK-001)
- Registered ClaudeCodeExecutor in ExecutorRegistry at module init (`__init__.py`)
- Rewrote `create_executor()` to delegate to `ExecutorRegistry.get()` instead of hardcoded if/else
- Changed canonical executor name from `"claude-code"` to `"claude_code"` (underscores, matching Python conventions)
- Updated `config.py` default `executor_type` from `"claude-code"` to `"claude_code"`
- Fixed singleton test isolation bug: `ExecutorRegistry.__new__()` returned the singleton, so test fixtures clearing `_executors` corrupted global state. Fixed by using `object.__new__()` in test fixtures.
- TDD: 11 new tests in `backend/tests/core/executor/test_claude_code_protocol.py` (protocol compliance, registration, create_executor integration)
- Updated existing tests in `test_executor_claude_code.py` to use new executor name and KeyError
- All 897 tests pass, coverage 93.64%, ruff clean
- Files changed: `__init__.py` (updated), `config.py` (updated), `test_claude_code_protocol.py` (new), `test_registry.py` (fixture fix), `test_executor_claude_code.py` (updated)

## TASK-004: Add executor_type and executor_metadata fields to Task model
- Added `executor_type: str` (default='coding', server_default='coding') to Task model for executor routing
- Added `executor_metadata: dict` (default={}, server_default='{}') to Task model for executor-specific configuration
- Named `executor_metadata` instead of `metadata` because SQLAlchemy reserves `metadata` attribute name on declarative models
- Created Alembic migration `l4g5h6i7j8k9_add_executor_type_and_metadata_to_tasks.py`
- Updated schemas: `TaskCreate`, `TaskUpdate`, `TaskResponse` all include both new fields with defaults
- TDD: 15 tests in `backend/tests/test_task_executor_fields.py` (5 model, 10 schema)
- All 923 tests pass, coverage 93.65%, ruff clean
- Files changed: `models.py`, `schemas.py`, migration file (new), `test_task_executor_fields.py` (new)

## TASK-005: Update TaskRunner to use ExecutorRegistry
- Changed `LocalTaskRunner.__init__` from `executor: BaseExecutor` to `executor_name: str = "claude_code"` with optional `executor:` kwarg for backward compat
- Added `_resolve_executor(executor_type)` method: tries task's executor_type from registry, falls back to default executor_name
- `execute_task` and `review_task` now read `task.executor_type` and resolve executor per-task via registry
- Updated callers: `pm.py` and `cli/_helpers.py` now pass `executor_name=settings.executor_type` instead of `create_executor()`
- Backward compatible: existing tests pass unchanged using `executor=` kwarg
- TDD: 10 new tests in `backend/tests/test_task_runner_registry.py` (init, resolve, execute, review, backward compat)
- All 922 tests pass, coverage 93.65%, ruff clean
- Files changed: `task_runner.py` (updated), `pm.py` (updated), `cli/_helpers.py` (updated), `test_task_runner.py` (updated imports), `test_task_runner_registry.py` (new)

## REVIEW-001: Code review — fix all issues including minor ones
- Ran `ruff check backend/src/` — all checks passed
- Ran `pytest backend/tests/ --cov=backend/src --cov-fail-under=80` — 922 passed, 93.65% coverage
- Reviewed all 22 changed files on the branch
- Fixed CRITICAL: `.env.example` (root + deploy) and `docker-compose.yml` still had `EXECUTOR_TYPE=claude-code` (old hyphenated name), updated to `claude_code`
- Fixed unused imports in 4 test files: `test_claude_code_protocol.py` (ExecutionResult, ReviewResult), `test_protocol.py` (runtime_checkable, AsyncMock, pytest), `test_task_executor_fields.py` (TaskType, TaskSource), `test_task_runner_registry.py` (TaskExecutionResult, TaskReviewResult)
- Verified: ruff clean on src/ and all changed test files, 922 tests pass, 93.65% coverage
- Files changed: `.env.example`, `deploy/.env.example`, `deploy/docker-compose.yml`, 4 test files (import cleanup)
- Fixed import order in `claude_code.py`: moved `structlog` (third-party) after stdlib imports (`asyncio`, `re`, `shutil`, `sys`)
- Verified: protocol compliance correct, thread-safe registry, backward compatibility with getattr fallback, no security issues, clean imports
- All tests still pass after fix, coverage unchanged at 93.65%
- Files changed: `claude_code.py` (import reorder)
## Completed at 2026-03-28T09:24:30Z after 6 iterations

---

## TASK-001: GatewayProvider protocol + implementations
- Created `backend/src/providers/` package (new)
- Defined `GatewayProvider` protocol in `providers/gateway.py` using `@runtime_checkable` Protocol
- `ChatCompletionResult` dataclass: content, model, usage fields
- Protocol method: `async chat_completion(messages, model_hint, task_metadata) -> ChatCompletionResult`
- Implemented `BSGatewayProvider`: routes requests through BSGateway API
  - Sends POST to `{base_url}/v1/chat/completions` with Bearer auth
  - Adds `X-BSNexus-*` headers from task_metadata dict
  - Uses httpx.AsyncClient for async HTTP
- Implemented `LiteLLMDirectProvider`: direct litellm.acompletion fallback
  - Falls back to default_model when model_hint is empty
  - Passes api_key and optional base_url to litellm
  - Extracts usage data from litellm response
- TDD: 17 tests written first in `backend/tests/providers/test_gateway.py`
  - 3 protocol compliance tests
  - 2 ChatCompletionResult tests
  - 5 BSGatewayProvider tests (success, no metadata, HTTP error, auth header, body)
  - 7 LiteLLMDirectProvider tests (success, model hint, default model, api key, error, metadata ignored, base_url)
- 100% coverage on gateway.py (49 statements, 0 missed)
- All 939 existing tests still pass, ruff clean
- Files created: `providers/__init__.py`, `providers/gateway.py`, `tests/providers/__init__.py`, `tests/providers/test_gateway.py`
- Key decisions: self-contained module (no cross-provider imports), structlog for logging, no settings import (config passed at construction time)

## TASK-002: SupervisorProvider protocol + implementations
- Created `backend/src/providers/supervisor.py` with `SupervisorProvider` protocol (`@runtime_checkable`)
- Protocol methods: `async log_event(agent_id, event_type, data) -> None`, `async check_permission(agent_id, action) -> bool`
- Implemented `BSupervisorProvider`: HTTP client to BSupervisor API
  - `log_event` POSTs to `{base_url}/api/v1/events` with Bearer auth and JSON body
  - `check_permission` POSTs to `{base_url}/api/v1/permissions/check`, returns `resp["allowed"]`
  - Uses httpx.AsyncClient for async HTTP, structlog for logging
- Implemented `NoOpSupervisorProvider`: no-op fallback
  - `log_event` does nothing (silent no-op)
  - `check_permission` always returns True (permissive fallback)
- TDD: 18 tests written first in `backend/tests/providers/test_supervisor.py`
  - 4 protocol compliance tests (runtime_checkable, compliant, non-compliant, partial)
  - 8 BSupervisorProvider tests (log_event success/no-data/error/auth, check_permission allowed/denied/error/auth)
  - 6 NoOpSupervisorProvider tests (log_event no-op, check_permission always true, protocol compliance)
- All 957 tests pass, coverage 93.76%, ruff clean
- Files created: `providers/supervisor.py`, `tests/providers/test_supervisor.py`
- Self-contained module, no cross-provider imports, config passed at construction time

## TASK-003: KnowledgeProvider protocol + implementations
- Created `backend/src/providers/knowledge.py` with `KnowledgeProvider` protocol (`@runtime_checkable`)
- Protocol methods: `async get_sot(project_id) -> dict`, `async store_result(task_id, result) -> None`, `async search(query, limit) -> list[dict]`
- Implemented `BSageProvider`: HTTP client to BSage API
  - `get_sot` GETs from `{base_url}/api/knowledge/sot/{project_id}` with Bearer auth
  - `store_result` POSTs to `{base_url}/api/knowledge/results/{task_id}` with task_id and result in body
  - `search` POSTs to `{base_url}/api/knowledge/search` with query and limit, returns `resp["results"]`
- Implemented `LocalMarkdownProvider`: local filesystem fallback
  - `get_sot` reads `{knowledge_dir}/sot/{project_id}.md`, returns empty dict if not found
  - `store_result` writes JSON to `{knowledge_dir}/results/{task_id}.md`, creates dir if needed
  - `search` scans `{knowledge_dir}/docs/*.md` for case-insensitive match, respects limit
- TDD: 18 tests in `backend/tests/providers/test_knowledge.py`
- 100% coverage on knowledge.py (67 statements, 0 missed)
- All 975 tests pass, coverage 93.86%, ruff clean
- Self-contained module, no cross-provider imports, config passed at construction time

## TASK-004: Provider config via pydantic-settings
- Added provider selection fields to `Settings` in `backend/src/config.py`:
  - `gateway_provider: Literal["bsgateway", "litellm"]` (default: "litellm")
  - `supervisor_provider: Literal["bsupervisor", "noop"]` (default: "noop")
  - `knowledge_provider: Literal["bsage", "local"]` (default: "local")
- Added URL and API key configs: `bsgateway_url`, `bsgateway_api_key`, `bsupervisor_url`, `bsupervisor_api_key`, `bsage_url`, `bsage_api_key`
- Added `knowledge_dir` for local knowledge directory (default: "./knowledge")
- Used `Literal` types for provider selection — invalid values raise `ValidationError`
- All defaults are fallback providers (litellm, noop, local) — no external dependencies required
- Updated `.env.example` with all new config keys and comments
- TDD: 20 tests in `backend/tests/providers/test_provider_config.py`
  - 10 default value tests, 7 env var override tests, 3 validation tests
- All 995 tests pass, coverage 93.87%, ruff clean
- Files changed: `config.py` (updated), `.env.example` (updated), `test_provider_config.py` (new)

## TASK-005: FastAPI dependency injection for providers
- Created `backend/src/providers/dependencies.py` with factory and DI functions
- Factory functions: `create_gateway_provider()`, `create_supervisor_provider()`, `create_knowledge_provider()`
  - Each reads `settings` to resolve the configured provider implementation
  - Returns fallback providers by default (LiteLLMDirect, NoOp, LocalMarkdown)
- DI functions: `get_gateway_provider()`, `get_supervisor_provider()`, `get_knowledge_provider()`
  - Wrapped with `@functools.lru_cache(maxsize=1)` for singleton caching
  - Compatible with `FastAPI Depends()` and `app.dependency_overrides` in tests
- TDD: 13 tests in `backend/tests/providers/test_dependencies.py`
  - 3 gateway factory tests (litellm default, bsgateway, fallback)
  - 2 supervisor factory tests (noop default, bsupervisor)
  - 2 knowledge factory tests (local default, bsage)
  - 3 DI function tests (returns correct type for each provider)
  - 3 override tests (verifies `app.dependency_overrides` works for test mocking)
- 100% coverage on dependencies.py (35 statements, 0 missed)
- All 1008 tests pass, coverage 93.92%, ruff clean
- Files created: `providers/dependencies.py`, `tests/providers/test_dependencies.py`
- Self-contained module, uses structlog for logging, settings patched in tests

## REVIEW-001: Code review — fix all issues including minor ones
- Verified all provider files are self-contained (no cross-provider imports between gateway.py, supervisor.py, knowledge.py)
- Only dependencies.py imports from provider modules (by design — it's the factory/DI layer)
- Ran `ruff check src/` — all checks passed
- Ran `ruff check tests/` — found 13 lint issues, fixed all:
  - 9 auto-fixed unused imports (pytest, dataclass, os, Literal, patch, select, AsyncClient, AuditLog)
  - 1 manually fixed: unused variable `deleted_task` in test_api_architect.py (changed to `_ = redesign_tasks[1]`)
  - 3 manually fixed: E402 module-level imports not at top of file in test_api_projects.py (added `# noqa: E402`)
- Ran full test suite: 1008 passed, 0 failed, 93.92% coverage (well above 80% threshold)
- All fallback providers work without external services (LiteLLMDirect, NoOp, LocalMarkdown)
- All providers use typing.Protocol with @runtime_checkable
- All provider implementations pass protocol compliance tests
