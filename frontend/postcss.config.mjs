// Tailwind 4 — config-less PostCSS plugin. ``@tailwind`` directives are
// replaced by ``@import "tailwindcss"`` at the top of globals.css.
export default {
  plugins: { '@tailwindcss/postcss': {} },
};
